# Copilot Instructions

## Project Guidelines
- User prefers comprehensive validation annotations on ViewModels and DTOs that match entity definitions. Should include Required, StringLength, Phone, EmailAddress, DataType, RegularExpression, FileExtensions attributes with custom error messages and Display names for better UX.